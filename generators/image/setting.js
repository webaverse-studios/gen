import {generateImage} from './generate-image.js';

export const generateSettingImage = generateImage({
  modelName: 'webaverse_locations',
  prefix: `Location concept art, tranding on ArtStation.`,
});